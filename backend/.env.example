# ==============================================================================
# K8s Assistant Backend Environment Configuration
# ==============================================================================
# 复制此文件为 .env 并填入实际值
# cp .env.example .env
# ==============================================================================

# ------------------------------------------------------------------------------
# Google Gemini AI Configuration (必需)
# ------------------------------------------------------------------------------
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-1.5-flash

# ------------------------------------------------------------------------------
# Alert Webhook Configuration (新增 - 告警推送)
# ------------------------------------------------------------------------------
# 告警 Webhook URL (支持钉钉机器人、企业微信、Slack 等)
ALERT_WEBHOOK_URL=https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN
# ALERT_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook?key=YOUR_KEY

# 是否启用告警推送 (true/false)
ALERT_ENABLED=true

# Webhook 请求超时时间（秒）
ALERT_WEBHOOK_TIMEOUT=10

# ------------------------------------------------------------------------------
# Alert Filter Configuration (新增 - 告警去重与冷却)
# ------------------------------------------------------------------------------
# 告警冷却时间（秒），相同异常在此期间内不会重复发送
ALERT_COOLDOWN_SECONDS=300

# 聚合时间窗口（秒），同一 Deployment 的异常在此窗口内合并为一条
AGGREGATION_WINDOW_SECONDS=300

# ------------------------------------------------------------------------------
# Monitor Configuration (新增 - 监控扫描)
# ------------------------------------------------------------------------------
# 扫描间隔（秒）
MONITOR_SCAN_INTERVAL=60

# 要监控的 Namespace 列表，逗号分隔
# 留空表示监控所有 Namespace
MONITOR_NAMESPACES=

# ------------------------------------------------------------------------------
# Kubernetes Client Configuration
# ------------------------------------------------------------------------------
# 在集群内运行时会自动加载 ServiceAccount
# 本地开发时可指定 kubeconfig 路径
# KUBECONFIG=/path/to/kubeconfig
